/* jshint globalstrict: true */
'use strict';

// Just lodash wrapper functions
function cloneDeep(value) {
    return _.cloneDeep(value);
}

function isEqual(aValue, bValue) {
    return _.isEqual(aValue, bValue);
}